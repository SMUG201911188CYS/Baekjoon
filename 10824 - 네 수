#include <iostream>
#include <string>

using namespace std;

int main() {
	
	int a, b, c, d = 0;

	cin >> a >> b >> c >> d;
	
	string AB = to_string(a) + to_string(b);
	string CD = to_string(c) + to_string(d);
	
	
	cout << stol(AB) + stol(CD); // int로 할 시 범위가 넘어감.
	
	return 0;
}
