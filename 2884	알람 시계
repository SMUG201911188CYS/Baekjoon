#include <stdio.h>
#include <math.h>

int main(void)
{
	int a, b = 0;
	scanf("%d %d", &a ,&b);

	if (b - 45 < 0)
	{
		b = 60 + (b - 45);
		a--;
		if (a < 0)
		{
			a = 23;
		}
	}
	else
	{
		b = b - 45;
	}

	printf("%d %d", a, b);
	return 0;

}
