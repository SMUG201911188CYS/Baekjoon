#include <stdio.h>


int main(void)
{
	int num = 0;
	int a[100], b[100] = { 0, };
	scanf("%d", &num);
	for (int i = 0; i < num; i++)
	{
		scanf("%d %d", &a[i], &b[i]);
	}
	for (int j = 0; j < num; j++)
	{
		printf("Case #%d: %d + %d = %d\n", j + 1 ,a[j] ,b[j] ,a[j] + b[j]);
	}
	return 0;
}

Done.
