#include <stdio.h>
#include <stdbool.h>

int main(void)
{
	int a, b = 3;

	while (true)
	{
		scanf("%d %d", &a, &b);
		if (a == 0 && b == 0)
		{
			break;
		}
		printf("%d\n", a + b);
	}

	return 0;
}
Done.
