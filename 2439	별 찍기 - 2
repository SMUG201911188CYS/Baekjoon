#include <stdio.h>


int main(void)
{
	int num = 0;
	int a[100], b[100] = { 0, };
	scanf("%d", &num);
	for (int i = num; i > 0; i--)
	{
		for (int k = 0; k < i; k++)
		{
			printf(" ");
		}
		for (int j = num; j >= i; j--)
		{
			printf("*");
		}
		printf("\n");
	}
	return 0;
}
