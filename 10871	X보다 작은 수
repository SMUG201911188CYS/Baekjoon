#include <stdio.h>


int main(void)
{
	int a, b = 0;
	int c[10000] = { 0, }; // 배열 크기를 100으로 해놔서 틀렷음.
	scanf("%d %d", &a, &b);
	for (int i = 0; i < a; i++)
	{
		scanf("%d", &c[i]);
	}
	for (int j = 0; j < a; j++)
	{
		if (c[j] < b)
		{
			printf("%d ", c[j]);
		}
	}
	return 0;
}

Done.
